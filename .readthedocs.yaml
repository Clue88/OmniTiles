version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

sphinx:
  configuration: sdk/docs/conf.py

python:
  install:
    - method: pip
      path: sdk
      extra_requirements:
        - docs
